---
phase: 04-svg-board
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_web.py
autonomous: false

must_haves:
  truths:
    - "Board fragment is returned in HTMX score/undo responses"
    - "Board SVG contains meeple tokens at correct positions after scoring"
    - "Stacking offsets produce different coordinates when players share a cell"
    - "Lap badge markup appears when a player's score exceeds 49"
    - "Board displays correctly on a mobile viewport"
  artifacts:
    - path: "tests/test_web.py"
      provides: "Integration tests for board fragment"
      contains: "board"
  key_links:
    - from: "tests/test_web.py"
      to: "app/web/routes.py"
      via: "TestClient POST /score and /undo return board OOB"
      pattern: "id=\"board\""
---

<objective>
Add integration tests for the SVG board and verify the visual result on mobile.

Purpose: Ensure board tokens, stacking, laps, and OOB updates work correctly and don't regress.
Output: Passing test suite covering board rendering, plus visual confirmation on mobile viewport.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-svg-board/04-01-SUMMARY.md

# Existing test patterns -- follow the same TestClient + session override approach
@tests/test_web.py

# Routes being tested
@app/web/routes.py

# Dependencies with board data
@app/web/dependencies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Board integration tests and unit tests for build_board_context</name>
  <files>
    tests/test_web.py
  </files>
  <action>
Add tests to the existing `tests/test_web.py` file (or create `tests/test_board.py` if test_web.py is large). Follow the existing test patterns (TestClient, session overrides).

Tests to add:

1. **Unit test: build_board_context with empty players** -- returns empty dict.

2. **Unit test: build_board_context with single player at score 0** -- returns cell 0 with correct coordinates (528, 361), no stacking offset, initial letter, hex color, lap 0.

3. **Unit test: build_board_context with player at score 55** -- cell is 5 (55 % 50), lap is 1, coordinates match BOARD_CELLS[5].

4. **Unit test: build_board_context stacking** -- two players both at score 0 -> both in cell 0 but with different (cx, cy) due to radial offsets. Assert `bp1["cx"] != bp2["cx"]` or `bp1["cy"] != bp2["cy"]`.

5. **Integration test: dashboard GET includes board SVG** -- GET `/games/{id}` response contains `id="board"`, `class="board-svg"`, `viewBox="0 0 600 420"`, and the image href `/static/images/carcassonneok.jpg`.

6. **Integration test: score POST returns board OOB fragment** -- POST `/games/{id}/score` with valid data returns HTML containing `id="board"` with `hx-swap-oob="true"`. Also assert the response contains a `token-meeple` group for the scored player.

7. **Integration test: undo POST returns board OOB fragment** -- POST `/games/{id}/undo` returns board OOB with tokens at updated positions.

8. **Integration test: lap badge appears** -- Score a player past 49 (e.g., add 55 points), then GET dashboard. Assert response contains `class="lap-badge"` and text `x1`.

Follow the existing test setup pattern: create game, add players, start game, then test. Use the same session override pattern from existing tests.
  </action>
  <verify>
  `pytest tests/ -x -v` -- all tests pass, including the new board tests.
  </verify>
  <done>
  At least 6 tests pass covering: build_board_context (empty, single, lap, stacking), board in dashboard GET, board OOB in score/undo POST, and lap badge rendering.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SVG scoring board with meeple tokens, stacking, lap badges, and HTMX live updates. The board shows the real Carcassonne scoring track photo with player tokens moving based on scores.</what-built>
  <how-to-verify>
    1. Start the app: `docker compose up --build` (or `uvicorn app.main:app --reload`)
    2. Create a new game at http://localhost:8000/games/new
    3. Add 3+ players with different colors, start the game
    4. Verify the board photo appears with no tokens (all at cell 0)
    5. Score some points -- tokens should appear and move on the board
    6. Score two players the same amount -- their tokens should stack with offset (not overlap)
    7. Score a player past 49 points total -- a gold "x1" lap badge should appear
    8. Undo an action -- tokens should move back
    9. Open on mobile (or use browser DevTools mobile view) -- board should scale to fit, no horizontal scroll
    10. Score table, controls, and board should all update together after each action
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `pytest tests/ -x -v` -- all tests pass (including new board tests)
2. Visual inspection confirms board renders correctly with tokens, stacking, and lap badges
3. Mobile viewport shows board scaling correctly without horizontal scroll
</verification>

<success_criteria>
1. All integration tests pass covering board in GET, OOB in POST score/undo, stacking, and lap badges
2. Visual verification confirms the board works correctly on both desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/04-svg-board/04-02-SUMMARY.md`
</output>
