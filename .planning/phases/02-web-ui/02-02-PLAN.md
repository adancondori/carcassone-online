---
phase: 02-web-ui
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/web/routes.py
  - app/templates/dashboard.html
  - app/static/js/controls.js
autonomous: true

must_haves:
  truths:
    - "GET /games/{id} renders the full dashboard page with score table, scoring controls, and action bar"
    - "POST /games/{id}/score accepts player_ids, points, event_type, description via form data and returns HTMX fragments"
    - "POST /games/{id}/undo marks the last action as undone and returns updated HTMX fragments"
    - "Score table shows all players ranked by total score with name, color dot, score, cell (score%50), and lap (score//50)"
    - "HTMX fragment responses include score_table as primary target AND action_bar + controls as OOB swaps"
    - "After scoring, the controls form resets (no stale player/points selection)"
    - "After undo, the score table reflects recalculated totals immediately"
    - "Dashboard is usable on mobile: touch targets >= 48px, no horizontal scroll, controls stack vertically"
  artifacts:
    - path: "app/templates/dashboard.html"
      provides: "Dashboard template with named blocks: score_table, controls, action_bar"
      contains: "block score_table"
      min_lines: 80
    - path: "app/web/routes.py"
      provides: "Dashboard GET, score POST, undo POST routes with OOB fragment rendering"
      contains: "hx-swap-oob"
      min_lines: 100
    - path: "app/static/js/controls.js"
      provides: "Client-side ephemeral state: player selection, point buttons, custom input, submit validation"
      min_lines: 40
  key_links:
    - from: "app/web/routes.py (score_action, undo_action)"
      to: "app/services.py (add_score, undo_last)"
      via: "route handlers call service functions"
      pattern: "add_score|undo_last"
    - from: "app/web/routes.py (score_action, undo_action)"
      to: "app/templates/dashboard.html"
      via: "renders individual blocks via block_name parameter and concatenates with HTMLResponse"
      pattern: "block_name=.*score_table"
    - from: "app/templates/dashboard.html (score form)"
      to: "app/web/routes.py (score_action)"
      via: "hx-post submits form data to score endpoint"
      pattern: "hx-post.*score"
    - from: "app/templates/dashboard.html (undo button)"
      to: "app/web/routes.py (undo_action)"
      via: "hx-post submits to undo endpoint"
      pattern: "hx-post.*undo"
---

<objective>
Implement the scoring dashboard -- the core feature of the application. Users see the score table, select players and points, submit scoring actions via HTMX, and undo the last action. All updates happen without page reload using the HTMX multi-fragment OOB pattern.

Purpose: This is the primary user interaction surface. After this plan, users can play an actual game of Carcassonne using the scoreboard: score points, see rankings, and undo mistakes.
Output: Fully functional scoring dashboard with HTMX-driven score table, controls, and undo.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-ui/02-RESEARCH.md
@.planning/phases/02-web-ui/02-01-SUMMARY.md
@docs/prototype/dashboard.html
@docs/plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard template, scoring routes, and undo with HTMX OOB fragments</name>
  <files>
    app/web/routes.py
    app/templates/dashboard.html
  </files>
  <action>
    1. Create app/templates/dashboard.html extending base.html:
       Use the prototype (docs/prototype/dashboard.html) as the visual reference. Convert to Jinja2 with named blocks for HTMX fragment rendering.

       Structure:
       ```html
       {% extends "base.html" %}
       {% block title %}{{ game.name }} — Carcassonne{% endblock %}
       {% block content %}
       <div class="container">
           <!-- Header -->
           <div class="header">
               <div>
                   <div class="header-title">Carcassonne</div>
                   <div class="header-game">{{ game.name }}</div>
               </div>
               <div class="header-status">En juego</div>
           </div>

           <div class="main-workspace">
               <div class="main-column">
                   <!-- Score Table Block -->
                   {% block score_table %}
                   <div id="score-table" class="score-table-wrapper"
                        {% if oob %}hx-swap-oob="true"{% endif %}>
                       <table class="score-table">
                           <thead>
                               <tr>
                                   <th colspan="2">Jugador</th>
                                   <th>Score</th>
                                   <th>Casilla</th>
                                   <th>Vuelta</th>
                               </tr>
                           </thead>
                           <tbody>
                               {% for player in players %}
                               <tr>
                                   <td><span class="player-dot" style="background:{{ PLAYER_COLORS[player.color].hex }}"></span></td>
                                   <td>{{ player.name }}</td>
                                   <td class="score-value">{{ player.score_total }}</td>
                                   <td class="cell-value">{{ player.current_cell }}</td>
                                   <td class="lap-value">{{ "x" ~ player.lap if player.lap > 0 else "--" }}</td>
                               </tr>
                               {% endfor %}
                           </tbody>
                       </table>
                   </div>
                   {% endblock %}
               </div>

               <!-- Controls Block -->
               {% block controls %}
               <div id="controls" class="controls"
                    {% if oob %}hx-swap-oob="true"{% endif %}>
                   <form id="score-form"
                         hx-post="/games/{{ game.id }}/score"
                         hx-target="#score-table"
                         hx-swap="outerHTML">

                       <!-- Player selection (checkboxes) -->
                       <div class="control-section">
                           <div class="control-label">Jugador(es)</div>
                           <div class="player-chips">
                               {% for player in all_players %}
                               <label class="player-chip" data-player-id="{{ player.id }}">
                                   <input type="checkbox" name="player_ids" value="{{ player.id }}" style="display:none">
                                   <span class="chip-dot" style="background:{{ PLAYER_COLORS[player.color].hex }}"></span>
                                   {{ player.name }}
                               </label>
                               {% endfor %}
                           </div>
                       </div>

                       <!-- Event type (radio buttons) -->
                       <div class="control-section">
                           <div class="control-label">Tipo</div>
                           <div class="type-buttons">
                               <label class="type-btn active">
                                   <input type="radio" name="event_type" value="CITY_COMPLETED" checked style="display:none">
                                   Ciudad
                               </label>
                               <label class="type-btn">
                                   <input type="radio" name="event_type" value="ROAD_COMPLETED" style="display:none">
                                   Camino
                               </label>
                               <label class="type-btn">
                                   <input type="radio" name="event_type" value="MONASTERY_COMPLETED" style="display:none">
                                   Monasterio
                               </label>
                               <label class="type-btn">
                                   <input type="radio" name="event_type" value="MANUAL" style="display:none">
                                   Manual
                               </label>
                           </div>
                       </div>

                       <!-- Points -->
                       <div class="control-section">
                           <div class="control-label">Puntos</div>
                           <div class="point-buttons">
                               <button type="button" class="point-btn" data-points="1">+1</button>
                               <button type="button" class="point-btn" data-points="2">+2</button>
                               <button type="button" class="point-btn" data-points="3">+3</button>
                               <button type="button" class="point-btn" data-points="4">+4</button>
                               <button type="button" class="point-btn" data-points="5">+5</button>
                               <button type="button" class="point-btn" data-points="8">+8</button>
                               <button type="button" class="point-btn" data-points="9">+9</button>
                               <button type="button" class="point-btn" data-points="10">+10</button>
                               <div class="point-custom">
                                   <input type="number" id="custom-points" placeholder="Otro" min="1" max="999">
                               </div>
                           </div>
                           <!-- Hidden input holds the actual points value submitted -->
                           <input type="hidden" name="points" id="points-input" value="">
                       </div>

                       <!-- Note -->
                       <div class="control-section">
                           <input type="text" class="note-input" name="description"
                                  placeholder="Nota opcional (ej: ciudad del norte)" maxlength="100">
                       </div>

                       <!-- Submit -->
                       <div class="control-section">
                           <button type="submit" class="btn-score" id="btn-score" disabled>
                               Anotar puntos
                           </button>
                       </div>
                   </form>
               </div>
               {% endblock %}
           </div>

           <!-- Action Bar Block -->
           {% block action_bar %}
           <div id="action-bar" class="action-bar"
                {% if oob %}hx-swap-oob="true"{% endif %}>
               <button class="btn-action"
                       hx-post="/games/{{ game.id }}/undo"
                       hx-target="#score-table"
                       hx-swap="outerHTML"
                       {% if not has_actions %}disabled{% endif %}>
                   <span class="icon">&#8630;</span>
                   Deshacer
               </button>
           </div>
           {% endblock %}
       </div>
       {% endblock %}

       {% block scripts %}
       <script src="/static/js/controls.js"></script>
       {% endblock %}
       ```

       IMPORTANT implementation details:
       - `players` is sorted by score (for the score table display)
       - `all_players` is sorted by turn_order (for the controls player chips -- consistent order regardless of score)
       - The `oob` context variable controls whether OOB attributes are rendered (False for full page, True for fragment responses)
       - Point buttons are type="button" (NOT submit) -- they set the hidden #points-input value via JS
       - Player chips use checkboxes for multi-select
       - Event type uses radio buttons for single-select

    2. Add dashboard and scoring routes to app/web/routes.py:

       Update GET /games/{game_id} route (replace the stub from 02-01):
       - Load game_state via get_game_state(session, game_id)
       - If game.status == "setup", redirect to /games/{game_id}/setup
       - Prepare all_players = sorted copy of players by turn_order
       - Render dashboard.html with context: game, players (sorted by score), all_players (sorted by turn_order), PLAYER_COLORS, has_actions=(action_count > 0), oob=False

       POST /games/{game_id}/score:
       - Use `def` (not async def)
       - Form params: player_ids: list[int] = Form(...), points: int = Form(...), event_type: str = Form(...), description: str = Form(None)
       - Build player_points = [(pid, points) for pid in player_ids]
       - Call add_score(session, game_id, player_points, event_type, description)
       - Reload game_state (fresh data after scoring)
       - Prepare context with oob=True for OOB fragments
       - Render 3 blocks and concatenate:
         ```python
         from fastapi.responses import HTMLResponse

         context = {
             "request": request, "game": game_state.game,
             "players": game_state.players,  # sorted by score
             "all_players": sorted(game_state.players, key=lambda p: p.turn_order),
             "PLAYER_COLORS": PLAYER_COLORS,
             "has_actions": game_state.action_count > 0,
             "oob": False,  # Primary target does NOT get oob attribute
         }

         # Primary target: score table
         score_html = templates.TemplateResponse(
             request, "dashboard.html", {**context, "oob": False},
             block_name="score_table"
         ).body.decode()

         # OOB: controls (reset form)
         controls_html = templates.TemplateResponse(
             request, "dashboard.html", {**context, "oob": True},
             block_name="controls"
         ).body.decode()

         # OOB: action bar (undo button state)
         action_bar_html = templates.TemplateResponse(
             request, "dashboard.html", {**context, "oob": True},
             block_name="action_bar"
         ).body.decode()

         return HTMLResponse(content=score_html + controls_html + action_bar_html)
         ```

       POST /games/{game_id}/undo:
       - Use `def` (not async def)
       - Call undo_last(session, game_id)
       - Reload game_state
       - Return same multi-fragment response as score route (score_table + OOB controls + OOB action_bar)
       - Same pattern: primary score_table (oob=False) + OOB controls + action_bar (oob=True)

       CRITICAL: render_dashboard_fragments() helper function:
       Extract the fragment rendering logic into a helper function `render_dashboard_fragments(request, templates, session, game_id)` to avoid code duplication between score and undo routes. Both routes call the same function after their respective service calls.

    3. Handle the form data parsing for player_ids correctly:
       FastAPI Form() for a list of checkboxes with the same name="player_ids" may need special handling. Test with multiple checkboxes. The standard approach is:
       ```python
       from fastapi import Form
       from typing import Annotated

       @router.post("/games/{game_id}/score")
       def score_action(
           request: Request,
           game_id: int,
           player_ids: Annotated[list[int], Form()],
           points: int = Form(...),
           event_type: str = Form(...),
           description: str = Form(None),
           session: Session = Depends(get_session),
       ):
       ```
       If `Annotated[list[int], Form()]` does not work for multi-value form fields, fall back to:
       ```python
       form_data = await request.form()
       player_ids = [int(pid) for pid in form_data.getlist("player_ids")]
       ```
       BUT since we use `def` (not async def), use the sync approach: parse from request directly or use the standard Form annotation.
  </action>
  <verify>
    Run the app and test manually or with curl:
    1. Create a game and add 2 players via the setup page
    2. Start the game
    3. curl -s http://localhost:8000/games/1 | grep "score-table"  # Dashboard renders
    4. curl -s -X POST http://localhost:8000/games/1/score -d "player_ids=1&points=8&event_type=CITY_COMPLETED" -H "HX-Request: true" | grep "score-table"  # Fragment returned
    5. Verify no "<!DOCTYPE html>" in fragment response (not a full page)
    6. curl -s -X POST http://localhost:8000/games/1/undo -H "HX-Request: true" | grep "hx-swap-oob"  # OOB fragments present
  </verify>
  <done>
    Dashboard renders with score table, controls, and action bar. POST /score updates the score table via HTMX fragment swap with OOB controls and action bar. POST /undo reverses last action and returns updated fragments. Controls form resets after each submission (via OOB swap). Undo button disabled when no actions exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Controls JavaScript for ephemeral UI state</name>
  <files>
    app/static/js/controls.js
  </files>
  <action>
    Create app/static/js/controls.js with vanilla JavaScript for the scoring controls:

    This script handles client-side ephemeral UI state ONLY. No game state, no scores, no persistence. It manages:
    1. Player chip toggling (checkbox visual state)
    2. Event type radio button visual state
    3. Point button selection and custom input
    4. Submit button enable/disable and label text
    5. Re-initialization after HTMX swaps

    ```javascript
    // ══════════════════════════════════════════════
    // Controls: ephemeral UI state for scoring form
    // ══════════════════════════════════════════════

    function initControls() {
        const controls = document.getElementById('controls');
        if (!controls) return;

        // ── Player chips (multi-select checkboxes) ──
        controls.querySelectorAll('.player-chip').forEach(chip => {
            chip.addEventListener('click', () => {
                const checkbox = chip.querySelector('input[type="checkbox"]');
                checkbox.checked = !checkbox.checked;
                chip.classList.toggle('active', checkbox.checked);
                updateSubmitButton();
            });
        });

        // ── Event type buttons (radio, single-select) ──
        controls.querySelectorAll('.type-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                controls.querySelectorAll('.type-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                const radio = btn.querySelector('input[type="radio"]');
                if (radio) radio.checked = true;
            });
        });

        // ── Point buttons (single-select, sets hidden input) ──
        const pointsInput = document.getElementById('points-input');
        const customInput = document.getElementById('custom-points');

        controls.querySelectorAll('.point-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                controls.querySelectorAll('.point-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                pointsInput.value = btn.dataset.points;
                if (customInput) customInput.value = '';
                updateSubmitButton();
            });
        });

        // Custom points input
        if (customInput) {
            customInput.addEventListener('input', () => {
                controls.querySelectorAll('.point-btn').forEach(b => b.classList.remove('active'));
                const val = parseInt(customInput.value);
                pointsInput.value = val > 0 ? val : '';
                updateSubmitButton();
            });
        }

        // ── Submit button state ──
        function updateSubmitButton() {
            const btn = document.getElementById('btn-score');
            if (!btn) return;

            const selectedPlayers = controls.querySelectorAll('.player-chip input:checked');
            const hasPoints = pointsInput && pointsInput.value && parseInt(pointsInput.value) > 0;
            const canSubmit = selectedPlayers.length > 0 && hasPoints;

            btn.disabled = !canSubmit;

            if (canSubmit) {
                const names = Array.from(selectedPlayers).map(cb =>
                    cb.closest('.player-chip').textContent.trim()
                ).join(', ');
                btn.textContent = `Anotar +${pointsInput.value} a ${names}`;
            } else {
                btn.textContent = 'Anotar puntos';
            }
        }
    }

    // Initialize on page load
    document.addEventListener('DOMContentLoaded', initControls);

    // Re-initialize after HTMX swaps (controls are OOB-swapped after each score/undo)
    document.body.addEventListener('htmx:afterSwap', (event) => {
        // Re-init whenever the controls div is swapped
        if (event.detail.target.id === 'controls' ||
            event.detail.elt.id === 'controls' ||
            document.getElementById('controls')) {
            initControls();
        }
    });

    // Also listen for OOB swaps specifically
    document.body.addEventListener('htmx:oobAfterSwap', (event) => {
        initControls();
    });
    ```

    IMPORTANT DETAILS:
    - The `initControls()` function is idempotent -- it re-binds events on the current DOM elements. Since OOB swap replaces the entire #controls div, old event listeners are garbage collected with the old DOM nodes.
    - The `htmx:afterSwap` event fires after HTMX inserts new content. We must re-init because the OOB swap replaces the controls div with fresh HTML (no event listeners).
    - Touch targets: all buttons should be >= 48px via CSS (already set in style.css from prototypes -- .point-btn is 48x44, .player-chip has 8px padding on 0.85rem text = ~40px height). If .player-chip height is < 48px, add min-height: 48px in CSS.
    - The custom points input clears point button active states and vice versa.

    After creating controls.js, verify that touch targets meet the 48px requirement in style.css. If any interactive element is below 48px, update style.css to add min-height: 48px or increase padding.
  </action>
  <verify>
    test -f app/static/js/controls.js && echo "controls.js exists"
    wc -l app/static/js/controls.js  # Should be >= 40 lines
    grep -c "initControls" app/static/js/controls.js  # Should be >= 3 (definition + 2 event listeners)
    grep -c "htmx" app/static/js/controls.js  # Should be >= 1 (HTMX re-init listener)
  </verify>
  <done>
    controls.js handles all ephemeral UI state: player chip toggling, point button selection, custom input, event type selection, submit button enable/disable with dynamic label. Re-initializes automatically after HTMX OOB swaps. All touch targets meet 48px minimum.
  </done>
</task>

</tasks>

<verification>
1. Full scoring flow: open dashboard, select player(s), select event type, tap point button, submit -- score table updates without page reload
2. Multi-player scoring: select 2 players, score -- both scores update in one action
3. Undo: tap undo -- scores revert, undo button disables if no more actions
4. OOB fragments: after scoring, controls form resets (no stale selections), action bar undo button enables
5. Custom points: type a number in "Otro" input, submit works
6. Mobile usability: no horizontal scroll on 375px viewport, all buttons tappable
7. `pytest tests/ -v` still passes
</verification>

<success_criteria>
- Dashboard displays ranked score table with player name, color, score, cell, lap
- Scoring via HTMX updates score table + controls + action bar in one request
- Undo works and returns updated fragments
- Controls reset after each submission (OOB swap)
- Submit button shows dynamic label with selected players and points
- All interactive elements have >= 48px touch targets
- No full page reloads during scoring
</success_criteria>

<output>
After completion, create `.planning/phases/02-web-ui/02-02-SUMMARY.md`
</output>
