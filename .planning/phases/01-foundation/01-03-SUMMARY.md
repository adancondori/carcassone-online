---
phase: 01-foundation
plan: 03
subsystem: infra
tags: [alembic, fastapi, docker, sqlite, uvicorn, migrations]

# Dependency graph
requires:
  - phase: 01-foundation/01-01
    provides: SQLModel models (Game, Player, ScoreAction, ScoreEntry) and database layer (engine, create_db_and_tables)
provides:
  - Alembic migration framework with SQLite batch mode and initial schema migration
  - FastAPI application shell with lifespan-managed database initialization
  - Docker containerization with persistent SQLite storage via named volume
affects: [02-web-ui, all future phases requiring migrations or Docker deployment]

# Tech tracking
tech-stack:
  added: [alembic, uvicorn, docker]
  patterns: [alembic-batch-mode, fastapi-lifespan, named-volume-persistence]

key-files:
  created:
    - alembic.ini
    - alembic/env.py
    - alembic/script.py.mako
    - alembic/versions/531e052bb60d_initial_schema.py
    - app/main.py
    - Dockerfile
    - docker-compose.yml
    - .dockerignore
  modified: []

key-decisions:
  - "Named volume mounts data DIRECTORY (db_data:/code/data), not individual .db file, to support WAL mode journal files"
  - "user_module_prefix set to sqlmodel.sql.sqltypes. so autogenerated migrations reference SQLModel types correctly"

patterns-established:
  - "Alembic batch mode: render_as_batch=True in both online and offline migration contexts for SQLite ALTER TABLE support"
  - "FastAPI lifespan: asynccontextmanager pattern for startup (create tables) and shutdown (dispose engine)"
  - "Docker volume: named volume on /code/data directory for SQLite persistence across container restarts"

# Metrics
duration: 2min
completed: 2026-05-01
---

# Phase 1 Plan 3: Alembic Migrations, FastAPI Shell, and Docker Summary

**Alembic with SQLite batch mode creating 4-table schema, FastAPI lifespan app with health endpoint, Docker containerization with named volume for persistent SQLite**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-01T23:30:34Z
- **Completed:** 2026-05-01T23:32:57Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- Alembic initialized with batch mode and initial migration creating all 4 tables (game, player, score_action, score_entry) with all constraints
- FastAPI application shell with asynccontextmanager lifespan managing database initialization on startup and engine disposal on shutdown
- Docker and docker-compose configuration with named volume (db_data:/code/data) for SQLite persistence across container restarts

## Task Commits

Each task was committed atomically:

1. **Task 1: Configure Alembic with batch mode and generate initial migration** - `aa32564` (feat)
2. **Task 2: Create FastAPI app shell and Docker configuration** - `54a7c97` (feat)

## Files Created/Modified
- `alembic.ini` - Alembic config pointing to sqlite:///data/carcassonne.db
- `alembic/env.py` - Alembic env importing all models, using SQLModel.metadata, batch mode, sqlmodel type prefix
- `alembic/script.py.mako` - Migration template with sqlmodel.sql.sqltypes import
- `alembic/versions/531e052bb60d_initial_schema.py` - Initial migration creating all 4 tables
- `app/main.py` - FastAPI app with lifespan for DB init, health check endpoint
- `Dockerfile` - Python 3.12-slim container with pip install and uvicorn CMD
- `docker-compose.yml` - Service definition with named volume db_data:/code/data
- `.dockerignore` - Excludes .git, __pycache__, data/, .venv/

## Decisions Made
- Named volume mounts the entire data DIRECTORY (not individual .db file) to support SQLite WAL mode journal files (.db-wal, .db-shm)
- user_module_prefix set to "sqlmodel.sql.sqltypes." so autogenerated migrations use SQLModel-specific column types

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Docker daemon was not running on the host machine, so docker compose verification was skipped. The FastAPI app was verified directly with uvicorn on an alternate port (8000 was occupied), confirming the health endpoint returns {"status": "ok"} with HTTP 200. Docker files were validated structurally.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Alembic migration framework ready for future schema changes
- FastAPI app shell ready for route development in Phase 2
- Docker configuration ready for containerized deployment
- All existing model tests (11/11) continue to pass

---
*Phase: 01-foundation*
*Completed: 2026-05-01*
